version: 2
project_name: opentelemetry-operations-collector

snapshot:
  version_template: 

env:
  - LD_FLAGS=-s -w
  - CGO_ENABLED=0 # TODO: CGO is not supported in goreleaser yet.
  - BUILD_FLAGS=-trimpath
  - GOWORK=off

builds:
  - id: otelcol-middleware-gpu-linux
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    binary: otelcol-middleware-gpu
    dir: _build
    ldflags:
      - "-s -w"
    flags:
      - "-trimpath"
      - "-buildvcs=false"

  - id: otelcol-middleware-gpu-windows
    goos:
      - windows
    goarch:
      - amd64
    binary: otelcol-middleware-gpu
    dir: _build
    ldflags:
      - "-s -w"
    flags:
      - "-trimpath"
      - "-buildvcs=false"

nfpms:
  - package_name: otelcol-middleware-gpu
    contents:
      - src: otelcol-middleware-gpu.service
        dst: /lib/systemd/system/otelcol-middleware-gpu.service
      - src: otelcol-middleware-gpu.conf
        dst: /etc/otelcol-middleware-gpu/otelcol-middleware-gpu.conf
        type: config|noreplace
        file_info:
          mode: 0644
      - src: config.yaml
        dst: /etc/otelcol-middleware-gpu/config.yaml
        type: config|noreplace
        file_info:
          mode: 0644
    scripts:
      preinstall: preinstall.sh
      postinstall: postinstall.sh
      preremove: preremove.sh
    overrides:
      rpm:
        dependencies:
          - /bin/sh
    ids:
      - otelcol-middleware-gpu-linux
    formats:
      - deb
      - rpm
    umask: 0
    maintainer: Google Cloud Platform <google@google.com> # something better than this
    description: Minimal OpenTelemetry Collector for NVIDIA GPU telemetry, exporting to Middleware via OTLP
    license: Apache 2.0

archives:
  - id: "otelcol-middleware-gpu-tarball"
    ids:
      - "otelcol-middleware-gpu-linux"
      - "otelcol-middleware-gpu-windows"
    formats:
      - "tar.gz"
    name_template: "otelcol-middleware-gpu_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
        - "zip"

signs:
  - cmd: cosign
    signature: ${artifact}.sig
    certificate: ${artifact}.pem
    args:
      - "sign-blob"
      - "--output-signature"
      - "${signature}"
      - "--output-certificate"
      - "${certificate}"
      - "${artifact}"
      - "--yes"
    artifacts: archive
    env:
      - SIGSTORE_NO_CACHE=true
